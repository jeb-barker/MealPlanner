import React from "react";

const ScheduleChange = () => {
    return (
        <div>
            <h1>Page to request a different time or notify that you won't be eating on a certain day</h1>
        </div>
    );
};

export default ScheduleChange;